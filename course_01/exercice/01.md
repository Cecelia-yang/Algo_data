![[Pasted image 20260517153929.png]]

> 注意这个地方high 必须是len(lst) - 1的长度，以及注意这种一次性两个赋值的方式。还有我们所有的操作都是由索引出发找数值的，一个是因为已经排列好顺序了，一个是因为所有的值是不固定的，但是写函数里里面的固定的东西是索引

![[Pasted image 20260517160013.png]]
1.计算步数的时候，要明确是原始值为零，运行一次while就往上加一次。然后初始值是一个起始点，只一次，说明因该在循环外面，所以steps = 0必须放在最开始的地方。然后要明白什么时候会运行while,什么时候会终止，就是只有找到那个值的时候，说明整个while里面的都是循环，所以steps+1不需要写在if语句里面，而是写在while这个语句下，与if等同等地位。所以整体是这个思路：（注意这个地方的可以与不可以，实际上的找到还是没找到）
![[Pasted image 20260517161122.png]]
![[Pasted image 20260517163329.png]]

1，注意重新调用自己写过的函数
2，这个地方要求输出布尔值类型，所以可以通过==一个等式是否相等==的思路来判断是正确的还是错误的。而等式的两端如何找数值判断，或者说哪个值在这个函数里面是被“输出拿到的”值--==return value==。注意这个形式。==布尔值的输出，不一定是非要明确“True”或者"False",== 而是一个结果

![[Pasted image 20260517172134.png|697]]
1.==注意不能直接写range,必须先转成list,因为这个染个只是生成一个虚拟的，而不能直接访问，而二分搜索的前提是必须可以随机访问，所以必须外面加一个List==
2，关注return value，注意上面函数是两个返回值，所以是idx, steps

![[Pasted image 20260517173517.png]]
1.注意，这个地方list里面要加range，因为size只是一个列表里面的数字
2.这个地方让target = size -1是因为这是最差结果，可以很好的看到效果
3.o(log n)最后是趋于平稳的（随着数据量的增大）
